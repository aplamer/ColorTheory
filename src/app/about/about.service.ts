export class AboutService{
    scrollUp(){
        window.scrollTo({top: 0, left: 0, behavior: 'smooth'});
    }

    scrollDown(){
        window.scrollTo({top: 870, left: 870, behavior: 'smooth'});
    }
}